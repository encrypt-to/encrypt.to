defaults: &defaults
  address:
  port:
  domain:
  user_name:
  password:
  authentication: 'plain'
  enable_starttls_auto: false
  openssl_verify_mode: 'none'
    
development:
  keyserver:
  secret_token: 
  <<: *defaults

test:
  <<: *defaults

production:
  keyserver:
  secret_token: 
  <<: *defaults

