<div class="l-content">

    <div class="information pure-g-r">
        <div class="pure-u-1-2">
            <div class="l-box">


<%= form_for(@user, :html => { :class => "pure-form pure-form-stacked" }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <h3><%= f.label :public_key %></h3>
    <%= f.text_area :public_key, :rows => 15, :cols => 75 %>
    <%= f.hidden_field :email %>
		
		<p class="alert" style="display:none;" id="invalid"></p>
		<p class="notice" style="display:none;" id="valid"></p>
	
    <br>
    <%= f.submit "Update", :class => "pure-button pure-button-primary", :disabled => "disabled", :id => "submit" %>

<% end %>

            </div>
        </div>

        <div class="pure-u-1-2">
            <div class="l-box">
                <h3 class="information-head">Public key owner</h3>
                <p><%#= @user.public_email %></p>
                <p><%#= @user.public_name %></p>
            </div>
        </div>
        
    </div> <!-- end information -->
</div> <!-- end l-content -->
