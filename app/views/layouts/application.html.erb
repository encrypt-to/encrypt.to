<!DOCTYPE html>
<html>
<head>
  <title>Encrypt.to | beta version</title>
	<meta http-equiv="Pragma" content="no-cache">
	<meta name="description" content="Send Encrypted Messages to 3.5 million PGP users by one click.">
	<meta name="keywords" content="pgp, openpgp, encryption, encrypt, privacy">
	<meta name="robots" content="index,follow">
  <script src="/assets/jquery.js?<%= Time.now.to_i %>" type="text/javascript"></script>
  <script src="/assets/jquery.form-validator.min.js?<%= Time.now.to_i %>" type="text/javascript"></script>
  <script src="/assets/openpgp.min.js?<%= Time.now.to_i %>" type="text/javascript"></script>
  <script src="/assets/<%= loadjs(request[:controller]) %>.js?<%= Time.now.to_i %>" type="text/javascript"></script>
  <script src="/assets/validate.min.js?<%= Time.now.to_i %>" type="text/javascript"></script>
 
  <link href="/assets/application.css?<%= Time.now.to_i %>" media="all" rel="stylesheet" type="text/css" />
  <link href="/assets/messages.css?<%= Time.now.to_i %>" media="all" rel="stylesheet" type="text/css" />
  <link href="/assets/pure-min.css?<%= Time.now.to_i %>" media="all" rel="stylesheet" type="text/css" />
 
  <%= csrf_meta_tags %>
</head>
<body>
	
	<% unless request[:controller] == "messages" %>
	<div>
	    <div class="hero-titles">
	        <h1 class="logo"><a href="/">En</a></h1>
	    </div>
	</div>
	<% end %>
	
	<% if flash[:notice] %>
	<p class="notice"><%= notice %></p>
	<% end %>
	
	<% if flash[:alert] %>
	<p class="alert"><%= alert %></p>
	<% end %>

  <%= yield %>
</body>
</html>
